name: .NET

on:
  - status
  - workflow_dispatch:

jobs:  
  build:
    if: github.event.context == 'AtomReleaseNotes/automated-build'
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2   
    - name: Hi there
      run: echo Hello world!
